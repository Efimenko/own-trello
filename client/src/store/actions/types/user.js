const prefix = 'USER'

export const userTypes = {
  REGISTER: `${prefix}:REGISTER`,
  LOGIN: `${prefix}:LOGIN`,
  LOGIN_BY_TOKEN: `${prefix}:LOGIN_BY_TOKEN`,
  LOG_OUT: `${prefix}:LOG_OUT`,
  ADD: `${prefix}:ADD`,
  CLEAR: `${prefix}:CLEAR`,
}
